export type PostType = {
  title: string,
  slug: string,
  period: string,
  desc1: string,
  desc2: string,
  desc3: string,
  thumbnail: string,
  contents: string,
  front: string[],
  back: string[]
}

export type StackType = {
  title: string,
  tag: string,
  image: string,
  contents: string
}